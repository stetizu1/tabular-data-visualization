/**
 * Array of opacities
 * First - opacity of all items when brushing is not active
 * Second - opacity of brushed items
 * Third - opacity of not brushed items
 */
export type Opacity = [number, number, number]
